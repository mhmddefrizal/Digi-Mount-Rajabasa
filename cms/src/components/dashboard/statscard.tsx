

interface Props {
  title: string;
  value: string;
  description: string;
  color: string;
}

// TODO: tambah icon untuk stats card
export default function StatsCard({
  title,
  value,
  description,
  color,
}: Props) {
  return ()
}